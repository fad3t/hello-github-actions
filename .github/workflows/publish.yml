name: publish
run-name: build is being published
on:
  workflow_run:
    workflows: [build]
    types: [requested]
    branches:
    - main
jobs:
  publish-first:
    runs-on: ubuntu-latest
    steps:
    - name: debug
      env:
        event: ${{ toJSON(github.event) }}
      run: echo "${event}"
